# Copy this file to .env and fill values before starting the backend
# Place this file in the backend folder you will run (project root or MGT/) and then run the server.

# MongoDB connection URL. Common formats:
# Atlas SRV (recommended for Atlas clusters):
#   mongodb+srv://<username>:<password>@clustername.mongodb.net/<dbname>?retryWrites=true&w=majority
# Standard (host:port):
#   mongodb://<username>:<password>@host:port/<dbname>?authSource=admin&retryWrites=true&w=majority
MONGODB_CONNECTION_URL=

# JWT secrets used across the app (set different secrets for each role in production)
accountant_Secret=change_me
casher_Secret=change_me
cashier_Secret=change_me
designer_Secret=change_me
workShop_Secret=change_me
reception_Secret=change_me
manager_Secret=change_me
superAdmin_Secret=change_me
inventoryClerk_Secret=change_me
# SMS / external tokens
sms_Tocken=
send_SMS_From=

# Troubleshooting SRV DNS errors (ENOTFOUND):
# 1) Verify your connection string cluster name is correct (no typos).
# 2) Test SRV DNS from PowerShell:
#    nslookup -type=SRV _mongodb._tcp.<your-cluster-host>
#    Example:
#    nslookup -type=SRV _mongodb._tcp.mgtbackup.6dvkviw.mongodb.net
#    If this returns "Non-existent domain" or no SRV records, your cluster host is wrong or DNS is blocked.
# 3) If SRV is not available, use the standard connection string with host:port (contact your DB admin)
# 4) Ensure your machine has internet access and that the hostname resolves. Some networks block DNS SRV.

# After editing, copy to .env:
#   copy .env.example .env
